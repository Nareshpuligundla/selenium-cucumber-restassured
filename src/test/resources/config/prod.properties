# ─── Production Environment Overrides ─────────────────────────────────────────
# Activate with: mvn clean test -Denv=prod

environment=prod
baseUrl=https://www.wm.com
apiBaseUrl=https://jsonplaceholder.typicode.com
headless=true
explicitWait=30
pageLoadTimeout=60
screenshotOnFailure=true
